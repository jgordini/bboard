# =============================================================================
# UAB Cloud.rc Deployment Environment Variables
# =============================================================================
# SECURITY: Never commit this file with real secrets!
# Copy this template to .env and fill in your actual values

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Public URL - use http:// and your instance IP (no domain/SSL for this deployment)
BASE_URL=http://138.26.48.197

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Generate strong password: openssl rand -base64 32
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
# Generate JWT secret: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_TO_STRONG_JWT_SECRET

# -----------------------------------------------------------------------------
# Email Configuration
# -----------------------------------------------------------------------------
EMAIL_NOREPLY=noreply@uab.edu

# Option A: UAB SMTP (contact UAB IT for credentials)
SMTP_HOST=smtp.uab.edu
SMTP_PORT=587
SMTP_USER=your-blazerid@uab.edu
SMTP_PASS=your-smtp-password

# Option B: MailHog for testing (no configuration needed)
# Uncomment these and comment out the UAB SMTP settings above
# SMTP_HOST=mailhog
# SMTP_PORT=1025
# SMTP_USER=
# SMTP_PASS=

# -----------------------------------------------------------------------------
# UAB SAML Authentication (optional - add after initial setup)
# -----------------------------------------------------------------------------
# Get SAML_IDP_CERT from UAB IT
# SAML_IDP_ENTITY_ID=https://idp.uab.edu/idp/shibboleth
# SAML_IDP_SSO_URL=https://idp.uab.edu/idp/profile/SAML2/Redirect/SSO
# SAML_IDP_CERT="-----BEGIN CERTIFICATE-----
# YOUR_UAB_IDP_CERTIFICATE_HERE
# -----END CERTIFICATE-----"

# -----------------------------------------------------------------------------
# Quick Setup Commands
# -----------------------------------------------------------------------------
# 1. Generate DB_PASSWORD:
#    openssl rand -base64 32
#
# 2. Generate JWT_SECRET:
#    openssl rand -base64 64
#
# 3. Copy this template:
#    cp .env.template .env
#
# 4. Edit .env with your values:
#    nano .env
#
# 5. Secure the file:
#    chmod 600 .env
